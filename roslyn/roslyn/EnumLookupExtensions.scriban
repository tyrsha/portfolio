// 🔥 이 파일이 생성될 최종 C# 코드 형태를 담당함
namespace EnumLookup.Generated
{
    public static class EnumLookupExtensions
    {
        public static {{ enumType }} To{{ enumType }}(this string name)
        {
            return name switch
            {
                {{ for item in assets }}
                "{{ item.key }}" => AssetCategory.{{ item.value }},
                {{ end }}
                _ => default
            };
        }
    }
}
